# Legacy Archive

This folder contains previous project layouts and historical artifacts kept for reference.

- `src-legacy/`: older FastAPI ledger implementation.
- `frontend-react/`: older React frontend implementation.
- `docs-root/`: previous root documentation and scripts.
- `data/` and `static/`: previous demo assets.

The active codebase now lives at repository root:

- `backend/`
- `frontend/`
- `docs/`
- `ml/`
- `infra/`
